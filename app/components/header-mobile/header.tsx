export default function MobileHeader(){
    return(
        <nav className="mobile-only fixed bottom-0 nav-height w-full flex border border-slate-500">
            mobile header
        </nav>
    )
}